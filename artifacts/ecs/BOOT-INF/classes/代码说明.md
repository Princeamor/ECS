###需要改进的代码
com.chitu.brain.conveyor.impl.ConveyorControllerServiceImpl findAutoIn


com.chitu.brain.car.impl.CarControllerServiceImpl getConveyorAndElevatorForMoveCar  同层移库链条机方向问题


#1.生成正式地图
update chitu_map_node set module_code='0',z=1;
update chitu_map_node set node_code=CONCAT("C-", x, "-",y,"-",z);
2.复制地图层数据
INSERT into chitu_map_node(node_code,node_type,x,y,z ,node_status,up,down,left_d,right_d,pallet_code,module_code,x_fd_count,x_nd_count,y_fd_count,y_nd_count,
map_node_type,chain_wait,run_num,create_time,update_time) select CONCAT(SUBSTRING(node_code, 1, LENGTH(node_code) - 1), '2') as node_code,node_type,x,y, 2 as z ,node_status,up,down,left_d,right_d,pallet_code,module_code,x_fd_count,x_nd_count,y_fd_count,y_nd_count,
map_node_type,chain_wait,run_num,create_time,update_time from chitu_map_node where z=1;







扫码器ip：192.168.10.95 2111

#空车换层调度
1.当前层没有可用车辆  没有车辆直接调度车子   有在运行的车辆是否需要调度  
2.没有车辆直接调度
3.有运行的车辆 调度过来的车辆没有停车位 如何处理
4.调度其他层车辆的算法：优先找其他层不在停车位的空闲车辆，其次是靠近当前层的

